# PWA Screenshots

Place screenshot images here for the manifest:

- `dashboard-mobile.png` (390x844) — Dashboard on iPhone
- `dashboard-wide.png` (1280x720) — Dashboard on Desktop

These are referenced in `/app/manifest.ts` and shown in install prompts on supported browsers.
